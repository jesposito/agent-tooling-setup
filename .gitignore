# ============================================================================
# STRICT DOT FILE POLICY: No dot files or directories in repository
# ============================================================================
# Exception: .gitignore and .git/ (required by Git)
# All configuration uses non-dot names (e.g., agent-tools.yaml.template)
# ============================================================================

# Ignore ALL dot files and directories (except .gitignore itself)
.*
!.gitignore

# User-specific configuration files
*.local.yaml
*.local.yml
agent-tools.yaml
.agent-tools.yaml
.agent-tools.local.yaml

# Environment variables (API keys, secrets)
*.env
*.env.*
.env*

# Testing and temporary files
test-temp/
*-test/
*.tmp
*.log

# Development/session documentation (keep local only)
SESSION-SUMMARY.md
ENHANCEMENTS-SUMMARY.md
agent-instructions.md
CLAUDE_CODE.md
AGENTS.md

# Migration and backup artifacts
*-backup-*/
*-last-backup
.agent-tools-migration-backup-*/
.agent-tools-last-backup

# OS-specific
.DS_Store
Thumbs.db
desktop.ini

# Editor and IDE
*.swp
*.swo
*~
*.bak

# === Agent Tooling - Auto-managed (DO NOT EDIT) ===
# Installation artifacts
.analysis-workspace/
.agent-tooling-temp/
.agent-tooling-cache/
agent-tooling-install.log

# API keys and secrets (CRITICAL - never commit!)
.env
.env.local
.env.*.local
.agent-tools.secrets.yaml

# Tool-specific temp/cache
.mem0-cache/
.gptme-cache/
.gptme-sessions/
.aider-cache/
.coderabbit-cache/

# Session files (may contain sensitive data)
.empirica-sessions/

# Per-project installation (if using local mode)
.agent-tools/

# === End Agent Tooling ===
# Dogfooding workspace (don't commit)
/agent-tooling-setup/
